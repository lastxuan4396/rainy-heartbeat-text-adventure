services:
  - type: web
    name: rainy-heartbeat-text-adventure
    runtime: static
    branch: main
    buildCommand: echo "No build step required"
    rootDir: text_adventure_love
    staticPublishPath: .
  - type: web
    name: hanzi-word-duel
    runtime: static
    branch: main
    buildCommand: rm -rf render-dist && cp -R content_hub_site render-dist
    rootDir: .
    staticPublishPath: ./render-dist
